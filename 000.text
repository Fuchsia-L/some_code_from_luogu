#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{
    vector<int> h;
    int x;
    while (cin >> x)
    {
        h.push_back(x);
    }
    vector<int>ans;
    for(int i=0;i<h.size();i++){
        if(ans.empty() || ans.back()>=h[i])
            ans.push_back(h[i]);
        else
            {
                auto it=upper_bound(h.begin(),h.end(),a[i],greater<int>());
                *it=a[i];
            }
    }
    cout<<ans.size();
    return 0;
}